[IST 22-JUN-2026 12:00:00] - Auto-detect CSV column formats in load_csv; save backtest results to dashboard/out/ inside the project
[IST 22-JUN-2026 14:00:00] - Fix UnboundLocalError for timedelta in strategy_44 and strategy_52
[IST 22-JUN-2026 15:00:00] - Compute PnL stats from entry/exit prices when strategies omit pnl_pips
[IST 22-JUN-2026 16:00:00] - Add scrollable candlestick chart with entry/exit markers below results chart
[IST 22-JUN-2026 17:00:00] - Refactor price chart to TradingView Lightweight Charts with native pan/zoom
[IST 23-JUN-2026 10:00:00] - Add BACKTEST INTEGRITY NOTICE comments to all 22 strategy files documenting leaks and fixes
[IST 23-JUN-2026 18:00:00] - Fix hindsight bias in strategies 06, 13, 17, 29, 31, 39, 42 using causal_backtest helpers
[IST 23-JUN-2026 12:00:00] - Fix minor hindsight in strategies 04, 08, 44 via causal_backtest helpers
[IST 23-JUN-2026 14:00:00] - Fix major hindsight bias in strategies 54, 56, 62, 69, 77, 78, 81 via causal_backtest (per-day/bar walks, simulate_exits)
[IST 23-JUN-2026 19:00:00] - Add causal_backtest.py shared helpers; fix CRITICAL strategies 01, 05, 28, 52, 65; close-only iFVG and confirmed swings in core.py
[IST 24-JUN-2026 15:45:00] - Fix dashboard backtest 500 when profit factor is infinite (JSON serialization)
[IST 24-JUN-2026 16:15:00] - Fix compute_volume_profile hang on gold/large-range instruments (capped session bins)
[IST 24-JUN-2026 16:30:00] - Speed up strategy 01 walk-forward; raise dashboard backtest timeout to 600s
[IST 24-JUN-2026 17:00:00] - Speed up strategy 06: per-day 1m scope, binary search, no full-file scans
[IST 24-JUN-2026 17:15:00] - Fix zero PnL when trade direction is bullish/bearish instead of long/short
[IST 24-JUN-2026 18:00:00] - Auto-load XAUUSD CSVs from Exness structured history when no upload provided
[IST 24-JUN-2026 19:00:00] - Add TradingView-style profit/loss boxes on price chart between entry and exit
[IST 22-JUN-2026 20:00:00] - Price chart: LWC v5 canvas trade zones primitive, OHLC legend, neobrutal theme
[IST 22-JUN-2026 21:00:00] - Align chart zones with reference: background-layer zones, autoscale SL/TP, debounced lazy load, pointer hover
[IST 22-JUN-2026 22:00:00] - Async backtest jobs with live progress UI; fix strategy 81 timeout via bisect + default 1y library window
[IST 22-JUN-2026 23:00:00] - Numba JIT layer (fast_core.py); O(log n) candle index lookups across all strategies; JIT MSS/resample/exits
[IST 22-JUN-2026 23:30:00] - Fix chart black band on hover by removing primitive drawBackground layer; cap zone autoscale
[IST 25-JUN-2026 01:30:00] - Fix price chart hover black band (remove Y clamping, restore background-layer zones) and restore trade zone/marker rendering for invalid open-trade exit times
[IST 25-JUN-2026 12:45:00] - Add automatic strategy concept overlays on price chart (VP, FVG, liquidity, sessions, fib, structure) with per-category toggles and active-trade filtering
[IST 25-JUN-2026 18:30:00] - Improve concept overlay label placement (compact tags, collision avoidance, trade-zone reservation); compact trade zone labels with hover detail; fix uploaded CSV backtests ignoring library window trim
[IST 25-JUN-2026 20:15:00] - Fix price chart hover black band: enable LWC autoSize, cream pane backgrounds, taller chart wrap, and integer sizing sync

[IST 27-JUN-2026 18:00:00] - Backtest realism overhaul: conservative intrabar exits (assume SL-before-TP when a bar straddles both); configurable round-turn trading-cost model (spread+commission+slippage) in core.enrich_trades_pnl with net-PnL-based win/loss; behavior-preserving causal-event caches (MSS/CISD/FVG) + zero-copy PrefixView to remove O(n^2) full-history rescans; fixed cache-defeating past_slice() patterns and per-bar full scans in strategies 17/29/31/39/42/56; added bt_metrics.py (R-multiples, expectancy, max drawdown), bt_runner.py / bt_aggregate.py harness. Result on XAUUSD (+EURUSD cross-check): 0/22 strategies net-profitable after costs. Added strategy_90_trend_breakout_atr.py (NEW trend-following Donchian+EMA+ATR-chandelier) which is net-positive and cost-robust (gold 1d +35R PF1.81 at 2x cost).

[IST 27-JUN-2026 20:30:00] - strategy_90 validation + dashboard wiring: added wf_validate.py (anchored walk-forward optimize-on-past, trade-next-segment, stitched OOS) and multi_instrument.py (all-instrument daily test). Result: walk-forward OOS net-positive on trending markets (BTC OOS +34R PF1.30, ETH +24R PF1.26, XAUUSD +20R PF1.89, USDJPY +7.5R PF1.42), negative on mean-reverting FX crosses (as expected). Refactored strategy_90 into importable generate_trades(); fixed dashboard discovery (parser.add_argument + 'Strategy 90:' name + --csv help='Daily') and set take_profit=exit_price for chart zones. Verified end-to-end via dashboard library backtest with YT_DATA_ROOT=./data (93 gold-daily trades, PF 1.80, chart session + E/X markers OK).

[IST 27-JUN-2026 22:00:00] - Forex-general MTF strategies + unbiased multi-pair walk-forward. strategy_91 (MTF liquidity-sweep continuation: 4H EMA bias + 1H sweep/reclaim + premium-discount) and strategy_92 (MTF mean-reversion to 4H fair value with sweep trigger). Downloaded 1H for all 16 FX pairs; wf_s91.py / wf_s92.py run anchored walk-forward (optimize-on-past, trade-next, stitched OOS) across every pair. RESULT (honest): s91 OOS net-positive 3/16, pooled -0.083 R/trade; s92 OOS net-positive 6/16 (crosses), pooled -0.021 R/trade. Neither is robustly profitable on FX after costs -> retail price-action/ICT geometry has ~zero gross edge on efficient FX majors; mean-reversion fits FX better than continuation. Both wired into dashboard (discovered, 1H arg). Only validated profitable edge remains trend-following (strategy_90) on trending instruments (gold/BTC/ETH/USDJPY), not FX.

[IST 28-JUN-2026 13:00:00] - Backtested Strategy 09 (MSS+OB, from Trade-Agent) honestly across 16 FX pairs. Copied missing scripts/utils/indicators.py dependency in; built bt_s09.py: causal (first-MSS-after-sweep, not live "most-recent" peek), conservative fills, per-pair costs, 15m execution, 2021+ window. RAW (no filters, = what their forex_backtester tests): net-positive 3/16 pairs, summed -3900R, pooled -0.37 R/trade over 10,440 trades; many pairs <10% win (broken 2R geometry). FILTERED (live entry_filters): filters reject ~99% of signals (10,440 -> 66 trades total across all pairs/5yr), still net -10.8R. Verdict: not profitable; filters create no edge, just near-zero trade frequency.


[IST 28-JUN-2026 17:00:00] - Faithful Strategy 09 replay (replay_s09.py) using EXACT live execution model: 1.5R TP, compute_smart_sl (OB extreme->worst 5M extreme->+15pip buffer), 16h bias TTL, strict-4H-EMA + London-open-block filters, real 5M execution per pair (5yr USD majors / EURUSD ~20yr / crosses ~8mo). Downloaded 5m + 15m + 1h for all 16 FX pairs. First pass looked positive (+124R, 13/16) but contained LOOKAHEAD: the 4H EMA filter read the in-progress (not-yet-closed) 4H candle. Fixed to use only closed 4H bars (matches live _drop_incomplete). Corrected result: FILTERED (live config) = -43.5R, only 7/16 pairs positive; EURUSD flipped 52.5%win/+56.5R -> 36%win/-20R/PF0.84. The strict-EMA filter fights the strategy's own pullback entries and only 'worked' via lookahead. RAW (no filter) sums +92.5R but concentrated/mixed across pairs, low frequency. Verdict: no robust live edge once lookahead removed.


[IST 28-JUN-2026 21:00:00] - Built strategy_93 (MTF Range Fade): mean-reversion to 4H EMA fair value, ICT sweep+reclaim trigger, REGIME filter (Kaufman efficiency ratio -> trade only in ranges, agrees with entry unlike S09), wide ATR-floored stops so fixed costs are a small fraction of risk. Isolated the core truth: range-fade has a REAL positive GROSS edge on FX (USDJPY +0.134R, EURUSD +0.041R gross) ? transaction cost is the deciding factor. Anchored walk-forward (optimize-on-past, OOS) across 16 pairs, wf_s93.py, with BT_COST_MULT cost-sensitivity. RESULT: at retail ~1.2pip cost -> OOS 4/16 positive, pooled -0.016 R/trade (net negative). At raw/ECN ~0.5pip cost -> OOS 9/16 positive, summed +127R, pooled +0.0204 R/trade (net POSITIVE out-of-sample). First FX strategy in project net-positive OOS, conditional on low costs; crosses + CHF pairs strongest. Added BT_COST_MULT to core.round_turn_cost_pips.


[IST 28-JUN-2026 23:30:00] - Refined raw S09 signal (refine_s09.py) WITHOUT lookahead: removed harmful 4H-EMA filter, added (a) PARTIAL profit at 0.5R + move stop to BREAKEVEN, (b) MSS-displacement quality gate (body>=0.10%, agrees with entry). Conservative stop-first sim, fair 1.5x round-turn cost on partialed trades. RESULT (16 FX pairs): FULL-sample 602 trades WIN 82.2% netExpR +0.27R (raw) / +0.22R (retail), 15/16 pairs profitable. OUT-OF-SAMPLE (held-out most-recent 40% per pair) 238 trades WIN 82.4% netExpR +0.29R (raw)/+0.25R (retail), 14/16 pairs profitable. Win profile: ~82% win, avg win ~0.5R, full loss -1R, expectancy +0.22-0.29R. USD majors (large samples) uniformly positive OOS. Caveats: crosses tiny samples; mild config-selection visibility; high win% comes from partial scratches not full-target hits.


[IST 29-JUN-2026 01:00:00] - Packaged the refined strategy as strategy_95_mss_ob_refined.py (dashboard-discoverable: --csv1h/--csv15m/--csv5m, 'Strategy 95:' docstring). Logic: 4H sweep bias -> displacement-gated 1H MSS (16h TTL) -> 15M OB(OTE) -> 5M tap -> smart SL -> partial 0.5R+breakeven -> final 1.5R. Reproduces validated numbers (USDCAD 88.4% win, GBPUSD 83.0%). Added partial-aware PnL: core.enrich_trades_pnl now honors a strategy-reported 'pnl_R' (net of 1.5x round-turn cost) so multi-leg-exit strategies show correct stats. Verified end-to-end via dashboard (YT_DATA_ROOT=./data): GBPUSD library backtest -> 94 trades, win 82.98%, PF 2.48, +780.8 pips, chart session + E/X markers render correctly.


[IST 29-JUN-2026 02:00:00] - strategy_95 now takes a raw 4H CSV (--csv4h) instead of resampling 4H from 1H ? no sampling, all 4 timeframes read raw. Downloaded native 4H for all 16 FX pairs. Dashboard now shows 4 upload slots (4h/1h/15m/5m) and resolves all four from the library. Verified raw-4H run matches the previous resampled run exactly (GBPUSD 94 trades, 83.0% win, +32.9R gross).


[IST 29-JUN-2026 03:00:00] - strategy_95 now emits a chronological events[] log per trade so the dashboard "Execution Steps" detail panel is populated: swept liquidity level (price + formation time), 4H sweep (price + side + bias), 15M order block (zone + fib + OTE), 1H MSS (break price + displacement %), 5M tap entry (entry/SL/TP), and exit (price + win/loss + R). Events sorted by timestamp (true chronological order). Each event has timestamp/type/price/description matching the dashboard renderer.


[IST 29-JUN-2026 04:00:00] - Chart hover black-window fix: trade-zones-primitive.js and concept-overlays.js drawBackground now fill in useBitmapCoordinateSpace (full device-pixel bitmap) instead of useMediaCoordinateSpace (CSS px), so the 2x retina canvas is fully painted and no longer shows through black on crosshair/hover redraws. Cleanup: removed local market data (data/, ~958M), dashboard/out/_tmp (~722M), bt_results/ (~651M), drive_listing.json and /tmp test files (~2.3GB freed). Code, dashboard, venvs and saved result JSONs retained. NOTE: dashboard library backtests now require re-downloading data or uploading CSVs.


[IST 29-JUN-2026 05:00:00] - strategy_95 now emits a two-leg exit breakdown in events[] so the partial-profit accounting reconciles in the detail panel: _simulate returns partial_time/partial_price; events include 'partial_take_profit' (Leg 1: half at +0.5R, stop->BE) and 'final_exit' (Leg 2: remaining half at target/BE with net R). Explains "entry==exit but +pips" (profit from the partial leg) and runners showing fewer pips than full entry->exit distance (only half rides to target). Verified on GBPUSD: 94 trades, 40 BE-scratch, 38 runners; event legs reconcile to displayed pnl. (No code regression ? results unchanged.)


[IST 29-JUN-2026 05:45:00] - Fixed pips=0 on pre-2000 trades (EURUSD 1999 data). core.MIN_VALID_TRADE_TS was 946684800 (2000-01-01), which made _trade_has_valid_exit_time() reject legitimate 1999 trades -> enrich_trades_pnl skipped PnL -> table showed 0 (while strategy's win/loss label remained). Lowered to 86400 (1970-01-02): rejects epoch-0/garbage only, allows all real history. Verified: 1999 trade now computes pnl_pips (e.g. +27.2 for 1R / 29-pip-risk), BE-scratch shows +5.4. Requires dashboard server restart (enrich runs in server process) + re-run backtest.


[IST 29-JUN-2026 06:30:00] - Resolved scale-out pips discrepancy in the trade table. Ran a real dashboard backtest (GBPUSD, library mode) and confirmed: scratches showed entry==exit with non-zero pips, runners showed (exit-entry) != pips, because a single entry/exit row can't represent the partial+breakeven two-leg exit. Fix: enrich_trades_pnl (pnl_R branch) now sets a BLENDED EFFECTIVE exit price so (exit-entry) reconciles EXACTLY to net pnl_pips for every row; real final fill kept in final_exit_price, real two legs kept in events[]. Verified: 0/94 rows mismatch, win_rate/total_pnl unchanged (82.98%, +780.8), chart markers still render. (Requires server restart + re-run; re-downloaded GBPUSD data with correct IDs into the library for testing.)


[IST 29-JUN-2026 08:00:00] - Dashboard: 3 features added.
(1) Saved Drive links: server persists used links to dashboard/out/saved_links.json keyed SYMBOL_TF (e.g. GBPUSD_1h) on each drive backtest; new GET /api/saved-links; drive request now carries 'symbol'. UI shows a per-timeframe "? saved" dropdown in the Drive inputs to refill links, refreshed after each drive run.
(2) Pair/Symbol input added (replaces hardcoded XAUUSD); library/local/drive backtests + library-status now use the typed symbol.
(3) Money panel (?): Starting Capital + Margin/Trade + Leverage(2000) inputs. Margin-based fixed-stake model: trade P&L(INR) = (net_price_move/entry) x leverage x margin (quote-ccy cancels). Trade table now shows P&L(?) and running Balance(?); summary shows Net P&L, Ending Balance, Return%, Max Drawdown. Computed in-browser so it updates live without re-running. Verified end-to-end on GBPUSD (drive run saved 4 links; money e.g. +20.9 pips -> +Rs3003).


[IST 02-JUL-2026 17:00:00] - Ported main-branch black band fix to live: scoped max-height to #resultsChart only (not all .chart-box canvas), disabled LWC autoSize with explicit resize via ResizeObserver, deferred chart init with whenChartMountReady until container has real height.
[IST 02-JUL-2026 22:25:00] - Liquidity sweep line now correctly bounded: mapLiquidityOverlays no longer extends liquidity_level_formed lines to entry. Instead, when a bearish_sweep/bullish_sweep is processed we draw the hline from the liquidity_level_formed timestamp to the sweep timestamp (the exact candle that was swept to the candle that swept it). Pending levels are tracked per-price and removed once swept. Other liquidity events keep prior behavior.
[IST 02-JUL-2026 22:55:00] - Fixed liquidity line vanishing on pan/zoom: bar-centre alignment now uses pixel offset at draw time (not fractional timestamps that break timeToCoordinate). Stopped rebuilding concept overlays on every visible-range change; primitive redraws itself on pan.
[IST 30-JUN-2026 23:00:00] - Built liveTrade/ ? 24/7 live runner for Strategy 95 on MT5/Exness (Windows). Reuses the exact strategy_95 detection (verified parity: live pipeline reproduces backtest entries, e.g. GBPUSD short 1.38955/SL1.39215/TP1.38565). Per-timeframe cadence on CLOSED candles only (4h/1h/15m/5m), staged bias->MSS->OB->tap with 16h TTL; independent per-TF logs (logs/<tf>.log) and per-TF pass files (passes/<tf>_passes.jsonl). Margin-based sizing (Rs1000 x 1:2000 via mt5.order_calc_margin, MAX_LOT cap). Management: partial 50% @ +0.5R -> stop to breakeven, runner to +1.5R TP; state persisted (resumes after restart). Email on start/execution/partial. Guards: DRY_RUN, one-trade-per-pair, max-concurrent, daily-loss pause. All config in .env (.env.example provided). Files: config/logging_setup/mt5_client/detection/notifier/trade_manager/engine/run + README + requirements. All modules import & compile clean off-Windows (MetaTrader5 guarded).
[IST 02-JUL-2026 23:35:00] - Fixed chart showing only the trade box with no candles: removed trade-zone full-pane drawBackground and top z-order (was painting over candles), stopped zone-only autoscale, merged visible candle highs/lows into autoscale band, and fixed trade jump viewport to keep entry candles in view.
[IST 04-JUL-2026 12:45:00] - Strategy 95: added optional strict 1H MSS causal mode (--strict-mss-causal) and dashboard toggle; default remains legacy timing, toggle on delays 5M entry until after the breaking 1H candle closes.
[IST 11-JUL-2026 00:20:00] - Fixed HTF-close/LTF-open lookahead in strategies 42 (8AM range after 1H close), 69 (4H liq +4h), 62 (1H BOS +1h), 28 (iFVG entry after HTF close via map_tf_bar_close_to_1m_idx), and 06 (walk-forward FVG/inversion, no forward windows). Batch XAUUSD ranking uses Strategy 95 --strict-mss-causal.

[IST 12-Jul-2026 15:48:52] - Added strategy_96_xau_trend_liquidity_trail.py (4H bias + 1H liquidity reclaim/Donchian + ATR chandelier trail) and Exness-calibrated backtest harness. Documented Exness XAUUSD pip=0.01 in core.py; BT_COST_PRICE=0.45. Best returns variant (no session filter) full history: PF 1.50, 982 trades, +327k Exness pips / +160R; beats s90/s91 baselines under same cost model.

[IST 12-Jul-2026 18:32:00] - Fresh Exness-cost batch of all 28 strategies (BT_COST_PRICE=0.45, no-resume) written to dashboard/out/batch_xauusd_exness*; s96 #1 total Exness PnL full+1y; s13 #1 PF; s28 timeout/fail.

[IST 13-Jul-2026 00:10:00] - Fetched origin/live strategies strategy_96_mss_ob_tuned.py + strategy_97_trend_meanreversion.py; renumbered local unified strategy to strategy_98_*; Exness XAUUSD compare: remote s96 PF0.93/-610 Exness pips (19 trades), remote s97 PF0.77/-27.7k Exness pips ? both underperform local s98.

[IST 13-Jul-2026 00:23:00] - Added root README.md documenting Exness pip/cost conventions, causality limits, XAUUSD leaderboard (s98 best total PnL, s13 best PF, remote s96/s97 not gold winners), and s96/s97/s98 ID map.

[IST 13-Jul-2026 00:26:00] - Added compare_s98_vs_s97_forex.py; fair multi-pair results under dashboard/out/compare_s98_vs_s97_forex (s97 best FX basket; s98 gold specialist).

[IST 13-Jul-2026 00:30:00] - Exness-aligned instrument specs in core.py (per-symbol pip/cost); fixed USDJPY heuristic pip 0.1?0.01; added audit_exness_pip_model.py; README contract-match table.

[IST 13-Jul-2026 00:42:00] - README: documented per-pair/per-strategy backtest date windows (note EURUSD 1999+ vs others 2021+).

[IST 13-Jul-2026 09:23:12] - Phased multi-pair backtest (3m/6m/1y/2y/3y from data end ~2026-07-10) via full_pair_compare_windows.py; mirrored summaries to dashboard/out/full_pair_compare_windows; README phased basket/XAUUSD/winner tables.


[IST 13-Jul-2026 11:30:56] - Added YouTuber mechanical proxies s99 (Power of Stocks Golden Setup), s100 (TopG structure S/D), s101 (Vinbull PA S/R), s102 (Techstreet level scalp); BTCUSD Exness pip/cost specs; compare_youtuber_strats.py vs s96/s97/s98 on 9 pairs (full/3m/6m/1y); README results. Gold still prefers s98; FX quality still s97; s99 inflates full basket via EURUSD length + high DD.

[IST 13-Jul-2026 11:38:05] - Split Power of Stocks into separate strategies: s99 Golden Setup, s103 5EMA, s104 inside-candle; added TopG CHOCH reversal as s105 (kept s100 continuation). Compare harness includes s99-s105. No merging of variants into one file.

[IST 13-Jul-2026 12:05:00] - Added YouTuber mechanical proxies s110-s115: TMT trendline bounce, MambaFX S/R breakout, Booming Bulls morning range + sniper retest (separate files), Stock Learners PDH/PDL sweep, Umar Punjabi Asia-London sweep+BOS. All causal 1H proxies with Source + Video URLs in docstrings.

[IST 13-Jul-2026 12:15:00] - Added YouTuber mechanical proxies s130-s139: JadeCap Daily Sweep + session FVG (s130/s131), Marco Trades liquidity trap + impulse CHOCH retest (s132/s133), Alex Temiz first-red-day fade + resistance lower-high (s134/s135), Andrea Cimi ORB + sweep-reclaim (s136/s137), Marci Silfrain top-down trend pullback (s138), Omor NBB 8:30 kill-zone FVG (s139). All causal 1H, Source + Video in docstrings; equity/tape/orderflow teachings proxied for FX/XAU/BTC.

[IST 13-Jul-2026 12:30:00] - Added Shreya FRX (@shreya.frx) Instagram ICT/SMC proxies: s106 London-session FVG pullback (4H EMA bias, UTC 07-10, 1.8R) and s107 liquidity-sweep + post-sweep FVG confluence (sweep 07-10, entry to 12:00, 2R). Source reels DVtL9R5j7k7, DVwY0WXkjYp, DV0tLCSjwID, DV8aW-cD40u, DWO-mtRD-Rt. Validated on EURUSD/USDCAD 1H.

[IST 13-Jul-2026 12:45:00] - Added YouTuber mechanical proxies s120-s127: Fabio Valentini IVB ORB (s120) + AMT failed-breakout mean reversion (s121); TG Capital Trident London model (s122); Umar Ashraf break-and-hold at PDH/PDL (s123); Brando Elite Options HTF S/R momentum breakout adapted for FX/gold (s124); Trader Kane Lab reversal (s125), Lab continuation (s126), PO3 50% manipulation (s127). Separate files per variant; causal 1H; Source + Video URLs in docstrings. Orderflow/SMT/ES-NQ divergence proxied with volume, FVG, sweep, and range logic.

[IST 13-Jul-2026 12:22:08] - Added compare_mega_traders.py harness (s106-s139 + baselines s96/s97/s98/s99 on GBPUSD/EURUSD/USDJPY/XAUUSD/BTCUSD; windows 3m+1y; s96 gold-only). Results mirrored to dashboard/out/compare_mega_traders. Key findings: s98 still #1 XAUUSD 1y (+77R); s131 best 5-pair basket (+131R 1y) and FX3 (+34R); s97 still best FX quality (avg R + lowest DD). README mega-expansion tables added.

[IST 13-Jul-2026 13:05:00] - README: added scannable "Which strategy where" guide (live s98 gold + s97 FX vs research s131/s132 basket, s99/s131 3m, s115/s139 sparse); aligned YouTuber/mega live-recommendation sections to explicit research-library framing.

[IST 13-Jul-2026 13:38:00] - liveTrade: wired Strategy 98 (XAUUSD 1H trend+liquidity+ATR trail) alongside legacy s95; STRATEGY_ID + FIXED_LOT in .env; detection_s98/engine_s98/trade_manager_s98; live deploy XAUUSD @ 0.01 lot (credentials from ExnessZeroLLMBOT path, gitignored .env).

[IST 13-Jul-2026 13:45:00] - liveTrade: added test_order_lifecycle.py — demo-only 0.01 lot smoke test for long/short place, SL trail, and close on XAUUSD (magic 989898, distinct from live s98 980098); verified PASS on Exness-MT5Trial8.

[IST 13-Jul-2026 23:45:00] - liveTrade s98: MAX_RISK_INR / MAX_RISK_PCT pre-trade guard (defaults Rs500 / 2% equity), S98_MAX_ENTRY_DELAY_SEC rejects late entries after restart, signal-bar freshness check, persisted last-signal state; fixed trade_email Strategy95 label for s98; test_risk_guard.py.

[IST 13-Jul-2026 23:33:52] - liveTrade risk caps: MAX_RISK_PCT raised to 15% (5-20% band), MAX_RISK_INR=0 so % alone applies; keep S98_MAX_ENTRY_DELAY_SEC=900 for late-restart protection. Updated .env / .env.example / config defaults.


[IST 14-Jul-2026 16:46:06] - liveTrade logs/cycle lines/emails use IST (UTC+05:30); candle scheduling and risk guards remain UTC.


[IST 14-Jul-2026 16:47:09] - liveTrade IST timestamps use ordinal human format (e.g. 16th July 2026 9:15:44 IST).


[IST 14-Jul-2026 22:26:46] - BTCUSD leakage-free strategy tournament: btc_research_engine.py, btc_strategy_candidates.py, btc_strategy_tournament.py, strategy_140_btc_walkforward_champion.py + unit tests. Select freeze: daily Donchian+ATR trail pyramid_max_3 (+67,395 net points on 2024-07-13..2025-07-12). Sealed final 2025-07-13..2026-07-12: -14,574 points (target 1.0-1.2M MISSED). Artifacts in dashboard/out/btc_strategy_tournament/. Research only; live gold remains s98.

[IST 14-Jul-2026 23:15:00] - BTC round-two statistical validation: btc_stat_validation.py + test_btc_stat_validation.py (calendar WF folds, purged CPCV, bootstrap CIs, DSR, PBO, eligibility gates, ranking, cost/delay stress). 31 unit tests passing.

[IST 14-Jul-2026 23:20:00] - Causal BTC feature store: btc_feature_store.py + test_btc_feature_store.py (event_time/available_time provenance, backward merge_asof joins, OHLCV/volatility, funding, 1m microstructure, on-chain/macro publication lags, ETH/cross-venue predictors, train-only preprocessing). 9 unit tests passing.

[IST 14-Jul-2026 23:30:00] - BTC round-two regime/model/candidate layers: btc_regimes.py, btc_models.py, btc_round2_candidates.py + focused tests (causal frozen regimes, calibrated meta-labelers with uplift selection, multi-TF candidate screen grid including microstructure and legacy wrappers). 19 unit tests passing.

[IST 14-Jul-2026 23:45:00] - BTC round-two orchestrator: btc_round2_tournament.py stages ingest/build-features/screen/validate/freeze/replay with --offline/--quick; artifacts under dashboard/out/btc_round2/; freezes strategy_141 only when eligibility gates pass.

[IST 15-Jul-2026 01:23:40] - BTCUSD round-two complete: engine repairs (HTF 1d->1w, fill-time stops, ranking gates), research .venv, data/feature/regime/model/stat modules, btc_round2_tournament pipeline. Freeze: no_deployable_champion (no s141). Best experimental squeeze_1d ~+155k OOS pts @ $25 RT; DSR/concentration gates failed. Artifacts dashboard/out/btc_round2/.

[IST 15-Jul-2026 13:30:33] - BTCUSD round-three research (no leverage): btc_round3_candidates.py, btc_stacking.py, btc_round3_tournament.py; engine pyramid_unlimited; hourly funding ingest fix; academic sleeves (seasonality/ITSM/TOC) net-negative after $25 RT; best stacked 1y ~368k pts @ peak 91 units (gap ~632k to 1M); status=no_deployable_champion (0 eligible, all fail DSR). Artifacts in dashboard/out/btc_round3/.

[IST 15-Jul-2026 15:23:06] - BTCUSD round-four complete (single-unit, no pyramiding): tracks max-capture / lead-lag / ML meta / aggTrades micro screened+validated under round-2 gates. Freeze status=frozen -> strategy_141_btc_adaptive_ensemble.py (regswitch_xwide_1d): OOS +141.9k @$25, DSR 0.814, PBO 0.091; 1y ~+75.6k (gap ~924k to 1M). Micro/lead-lag/ML failed to close gap. Artifacts dashboard/out/btc_round4/.
[IST 15-Jul-2026 17:50:00] - BTCUSD round-five: migrated research cache to O:\\D temp\\Data\\btc_research_data; fixed real-IS PBO + lead-lag coarse-join leakage; archived dead root scripts to scripts/archive/; ceiling analysis shows 1d impossible for 1M/yr; novel CUSUM/HMM/funding/volcomp/giveback/leadlag screened — status=no_deployable_champion (no s142); best ~60.5k 1y. Artifacts dashboard/out/btc_round5/.
[IST 15-Jul-2026 18:51:23] - BTCUSD round-six complete: fixed zig-zag ceiling + engine exits (max_hold/giveback/breakeven/flip) + independent event labels; PBO over all folds; data ingest (agg gaps, full 1m klines 2021-07..2026-07, ETH 1h); three tournament iterations over SAR/meta/CUSUM/cascade/router/infobar/ETH/path/EMA-ADX/BB-meta. status=no_deployable_champion (no s142); best r4_xwide_1d ~66.4k 1y (gap ~934k); irreducible under single-unit/$25 RT/gates. Artifacts dashboard/out/btc_round6/ + canvas btc-round6-research.canvas.tsx.

[IST 15-Jul-2026 23:42:08] - BTCUSD round-seven complete: engine flip/time-exit accounting + deferred breakeven; cost-aware EV gate; per-family PBO; Binance Vision 5m OI/L-S/taker metrics (~529k rows) + spot basis + DVOL + F&G; families OI-squeeze/basis/L-S/funding/EV-ML/jump/ensemble; three tournament loops + $5/$10 RT sensitivity. status=no_deployable_champion (no s142); best still r4_xwide_1d ~66.4k 1y (gap ~934k, below s141); novel derivatives only small honest positives. Artifacts dashboard/out/btc_round7/ + canvas btc-round7-research.canvas.tsx.

[IST 16-Jul-2026 01:35:00] - BTCUSD round-eight Phase-4 foundation: scripts/ingest_btc_round8.py (resumable premiumIndexKlines 1m Vision ingest, bookDepth probe, Bybit 1h, on-chain daily, Deribit skew/IV-RV builder, --smoke); btc_round8_candidates.py (12 families + R2/R3 wraps + feature/bar helpers); test_btc_round8_candidates.py (6 contract tests pass).

[IST 16-Jul-2026 03:45:00] - BTCUSD round-nine candidates foundation: btc_round9_candidates.py (28 signal families — classic TA, paper-driven, youtuber proxies, optional GP/LightGBM; FAMILY_BUILDERS + generate/run/grid); test_btc_round9_candidates.py (6 contract tests pass).


[IST 16-Jul-2026 06:07:28] - BTCUSD round-nine complete: R8 defect fixes + causal features; exhaustive TA/youtuber/paper/ML candidates (btc_round9_*); dual-track tournament (3 loops). status=no_deployable_champion (no s142); best elliott_15m ~120.9k 1y (fails family PBO/regime conc.); portfolio 9-sleeve optimistic ~438k 1y (fails mean DSR/PBO). Artifacts dashboard/out/btc_round9/ + canvas btc-round9-research.canvas.tsx.


[IST 16-Jul-2026 06:14:17] - Round-9 causality audit fixes: zigzag pivots now carry confirm_idx (no unconfirmed lookahead); ML sleeves train only on labels known at fit time; PDH/pivot points use full prior-day OHLC; run_round9_candidate auto-builds feature frames for freeze/S142. Regression tests added; honest tournament re-screen started.

[IST 16-Jul-2026 07:38:00] - Round-9 honest re-tournament complete after causality fixes (3 loops + cost/portfolio/freeze/report). status=no_deployable_champion (no s142); best experimental qhi_1h ~72.4k 1y (under R8 baseline ~76.9k); portfolio 12-sleeve optimistic ~394k 1y (fails mean DSR/PBO). Pre-fix elliott_15m ~120.9k discarded as leaky; docs/canvas updated.


[IST 17-Jul-2026 23:15:06] - Independent verification of drawdown_analysis_verified.md: real s98 ATR-trail gold remains profitable (+162.6R PF1.49 full / +82.3R PF1.97 1y); other doc judged a fixed-target reframe, not the strategy. Added verify_drawdown_claims.py + drawdown_analysis_response.md.

[IST 05-AUG-2026 18:30:00] - Add strategy_140_small_account_reversal.py: mechanical port of docs/"Small Account Futures Trading Strategy & Trade Execution Guide.pdf" (key 15m S/R zones from confirmed pivots + prev-day extremes, unhealthy parabolic flush via ATR displacement + leg efficiency, micro trend break, failed-breakout/double/H&S pattern + confirmation candle, buy/sell STOP entry, 2/3 scale-out at 1.5R -> break-even -> candle trail, 3R final). News filter dropped (no calendar data); US post-open timing window off by default (--session-start/--session-end). Fixed dashboard wiring broken by the strategies/ reorg: discover_strategies now scans root + strategies/, launches via the resolved path, and injects the repo root into PYTHONPATH so `from core import ...` resolves. Honest first result (GBPUSD 5m, full history, fair costs): 705 trades, win 45.7%, PF 0.64, -1756.8 pips -> no edge as literally specified; 15m variant -0.075R/trade.
[IST 05-AUG-2026 21:00:00] - Add strategy_141_mtf_zone_reclaim.py (original design, built from scratch): 3-degree stack derived from one CSV (HTF bias / MID zones / BASE execution); zone = origin candle of a displacement leg that closed through a CONFIRMED pivot and left an imbalance (not a swing high/low); fresh-zone-only with touch counting; BASE confirmation = zone reclaim + micro BOS + strong body; limit retest fill; hard min_rr gate vs the leg extreme; late partial + loose ATR trail + zone-invalidation exit. Plus scripts/validate_s141.py (pooled / per-symbol / leave-one-symbol-out / IS-OOS across 8 instruments and 3 TF stacks) and scripts/diag_s141.py (funnel counters). HONEST VERDICT: R:R half works and is stable (avg win 1.35-1.90R vs avg loss 0.85-0.96R) but the hit rate is 28-36% where >~36% is needed; best pooled -0.068R/trade (4h base, n=157). All variants negative (target 4x, bias 3x, regime 3x, entry 3x, imbalance 2x, invalidation 2x); leave-one-out negative in every cell; the HTF trend filter changes nothing measurable -> the zone carries no directional information on this data. NOT VALIDATED, documented as such in the file header. Independently reproduces the repo's earlier finding that SMC/ICT geometry has ~zero gross edge on FX majors.

[IST 05-Aug-2026 23:40:00] - Add/freeze strategy_142_mtf_pullback_reclaim.py: original Daily→4H persistence/pullback/reclaim strategy with next-bar stop entry, structural stop, fixed 0.65R target, and causal next-candle break-even after +0.50R MFE. Repaired scripts/validate_s142.py so development generation is physically truncated before the sealed 25% (the hidden path is not generated/costed), rejects unknown parameters and sealed sweeps; s142 now emits gross_R so the single-exit dashboard applies exactly one round-turn cost; exact final-exit event prices and same-candle dashboard exits were corrected. Frozen development after costs: TRAIN n=54, win 61.1%, +0.060R/trade, PF1.19; VALIDATION n=27, win 63.0%, +0.046R/trade, PF1.14. Sealed test opened exactly once with no retuning: n=20, win 65.0%, +0.183R/trade, PF1.82, +3.7R; 4/8 instruments positive. Status=SEALED-PASS PROVISIONAL due to small sample and limited breadth; not a live-profit guarantee.

[IST 06-Aug-2026 04:10:00] - Add strategy_143_mtf_supply_demand_reversal.py from scratch: causal 4H confirmed-pivot displacement/BOS origin zones -> fresh first 1H penetration/rejection -> closed 15m MSS -> independent 5m micro-BOS/origin-block limit retest; 1H sweep stop, opposing-4H-zone room gate, fixed 1.5R target, next-candle BE after +1R, conservative stop-first OHLC, one trade per fresh zone, no symbol exclusions. Dashboard takes --csv4h + --csv5m (1H/15m causally derived), renders 4H/5m order blocks, sweep, MSS, entry and exact exit; Drive temp filenames now retain the requested symbol so cost inference is correct. Downloaded repository-manifest Google Drive 5m histories and added validate_s143.py (8-symbol physically truncated 60/20/20 protocol), diag_s143.py and analyze_s143_dev.py. Latest 20% remains SEALED. Honest development after costs: TRAIN n36 win36.1% exp -0.090R PF0.82; VALIDATION n14 win50.0% exp +0.227R PF1.48. Target 1.10/1.25/1.50R and BE 0.50/0.75/1.00R neighborhoods had no variant positive in both partitions. Status=IMPLEMENTED / DASHBOARD-COMPATIBLE / NOT VALIDATED PROFITABLE; confirms extra MTF zone geometry did not automatically improve s142.

[IST 06-Aug-2026 05:28:00] - Add strategy_144_ny_orb_session_reversal.py from the supplied ORB/session rules: fixed UTC-5 Asian range (19:00-00:00), London sweep/push (02:00-08:00), exact NYSE 09:30-09:45 ORB from wall-clock 5m bars, one-sided sweep reversal plus both-sides continuation exception, 3-4 candle close-confirmed displacement, causal OB/FVG retrace with rejection/engulfing and next-bar-open entry, distal+ATR stop, session-liquidity obstruction/target logic, 1.8R default within tested 1.5-2.2R, 16:00 flat, one trade/day. Fixed a calendar aggregation defect discovered during diagnostics: generic resampling is file-origin anchored for some Drive files, so the exact 09:30/09:35/09:40 bars are aggregated directly. Added validate_s144.py (8-symbol Drive-backed physically truncated 60/20/20 protocol) and diag_s144.py. Sealed 20% remains unopened. Honest after-cost development: TRAIN n618 win41.4% exp -0.031R PF0.95; VALIDATION n125 win40.0% exp -0.080R PF0.87. All allowed targets 1.5/1.8/2.2R were negative in both pooled partitions. Dashboard smoke passed on GBPUSD (135 full-history trades + chart). Status=IMPLEMENTED / DASHBOARD-COMPATIBLE / NOT VALIDATED PROFITABLE.
[IST 13-Aug-2026 22:10:00] - s146 liveTrade robustness pass (4 fixes). (1) CONTENT-DERIVED ZONE IDS: strategy_146 zone_id was f"{timeframe}-{bos_index}-{direction}", an array slot that is only unique inside one fixed candle array; live the feed is a rolling window, so the same physical zone was renumbered every cycle. Measured damage in the 12-Aug logs: one EURAUD zone logged under 18 different ids, 204 zone_alert events for ~13 real zones, 468 4H bound-sets under multiple ids, and 3 of 9 traded 15m entry zones never journaled at all because the s146_events dedupe key "symbol:zone_id" suppressed a genuinely new zone that reused a retired slot (EURGBP 15m-3000-1 logged at 0.85330-0.85342 while the traded zone with that id was 0.85395-0.85408). Replaced with _zone_identity() = f"{timeframe}-{origin_time}-{direction}-{blake2s(bounds)[:6]}", invariant under windowing and across restarts. The two backtest sorts that tie-broke on the id string now tie-break on bos_index explicitly, so ordering no longer depends on id formatting - re-baseline the backtest, tie-break order may shift. (2) SCAN OVERRUN: engine warned "5m scan took longer than the 5m candle it runs on". Root cause was rebuilding 4H+15m zones from 3000 bars per symbol every 5 minutes although 4H structure changes once per 4h and 15m once per 15m, plus _destination_alive() refetching 4H history on every 15s poll per pending order. Added cached_zones() in detection_s146 keyed on (symbol, timeframe, swings) and invalidated by the last closed bar, a 4H-bar-slot cache for _destination_alive, fetch defaults cut to 4h=600/15m=1500/5m=1500 (600 4H bars = 100 days, ample for the new 7-day destination age cap; RAISE it if that cap is widened or disabled), and the warning now reports s/symbol plus the actual knobs. (3) UNMITIGATED VERIFICATION: added scripts/verify_s146_unmitigated.py, dependency-free, replays 4H CSVs against every logged destination_available POI using the backtest's own both-sides overlap test and reports overlap depth in pips. Run over 480 logged zones: 0 VIOLATION, 2 MARGINAL (NZDUSD clipped by 0.60 and 0.20 pips - inside the gap between two feeds, so the logic is not falsified, but mitigation is being decided by sub-pip margins and a tolerance would make the verdict feed-independent), 122 PARTIAL, 356 NO_DATA. Critically: 0 of the 8 POIs that actually produced trades could be checked, because data/ holds no 4H history for CHFJPY, EURAUD, EURCAD, EURCHF, EURGBP, GBPCAD, NZDCAD or NZDJPY - the real targets remain unverified against price. (4) OUTCOME LOGGING: a dry run emitted signal_found and nothing else (9 signals, 0 opened, 0 closed), so live expectancy was unmeasurable. Added TradeManagerS146.remember_paper()/_monitor_paper()/_resolve_paper(): suppressed signals are tracked and settled against closed 5m bars into paper_trade_opened/paper_trade_closed with exit_reason, r_multiple and bars_held, assuming stop-first when one bar straddles both levels so paper results never flatter the strategy. Entry is assumed at the trigger and spread/slippage are ignored; records are labelled mode=paper. All 6 touched files compile; zone-id stability, collision and determinism verified.
[IST 13-Aug-2026 23:55:00] - Add Strategy 147 (4H POI REACTION) as a new backtest + liveTrade stack, the opposite execution to s146: instead of travelling toward an unmitigated 4H zone, s147 waits for price to ARRIVE at a 4H zone and trades the reaction away from it. Sequence: 4H confirmed zone becomes a POI with NO age limit (an old untested zone is as valid as a fresh one, explicitly opposite to the s146 7-day cap) -> arrival detected on closed 15m bars -> 15m must confirm in order, first a change of character (close beyond the most recently confirmed opposing 15m swing) and only THEN a fresh aligned 15m zone which refines the entry -> 5m executes via aggressive liquidity sweep or conservative MSS. Lifecycle per user spec: a zone may be traded again on later visits (arrival episodes are separate; price must leave and return), and a zone is permanently invalidated once a 15m bar CLOSES fully beyond its distal edge. No holding limit: trades run to stop or target. Stop is placed beyond the 15m REFINEMENT zone distal edge, not the 4H zone, because a 4H zone can exceed 100 pips and a 1.5R target anchored there would never fill; target is a fixed 1.5R from the fill. KEY ACCOUNTING GUARANTEE (user requirement that a take profit must never display as a loss): core.enrich_trades_pnl derives win/loss from NET pips after round-turn costs, so a 1.5R target on a thin stop can book as a loss. s147 therefore REJECTS any setup whose gross target does not clear round_turn_cost_pips by S147_MIN_TARGET_COST_MULTIPLE (default 3.0), plus a min_stop_bps floor. Verified on the full GBPUSD 4H/15m/5m history through the dashboard's own pipeline: 517 trades, 209 take_profit ALL booked win, 308 stop_loss ALL booked loss, 0 misbookings, 0 open rows claiming PnL, all required dashboard fields present, directions strictly long/short. HONEST AFTER-COST RESULT: NOT PROFITABLE. GBPUSD full history win 40.4%, net -725.4 pips, expectancy -1.40 pips/trade, PF 0.81, mean realized +0.011R. An R sweep shows the target is NOT the problem - gross R stays ~breakeven at every level while win rate tracks the breakeven line almost exactly (1.0R 50.2% vs 50.0% needed; 1.5R 40.4% vs 40.0%; 2.0R 34.9% vs 33.3%; 2.5R 31.0% vs 28.6%; 3.0R 27.5% vs 25.0%), i.e. the entry timing carries no measurable edge on this symbol and costs consume the drift. Status=IMPLEMENTED / DASHBOARD-COMPATIBLE / PARITY-VERIFIED / NOT VALIDATED PROFITABLE. Files: strategies/strategy_147_4h_poi_reaction.py (self-contained, own swing/zone/liquidity primitives so s146 cannot be affected, content-derived zone ids from day one), liveTrade/s147_events.py (adds stamp() writing every timestamp as epoch + *_utc + *_ist so formation times are chart-readable straight off the log line, per user request), liveTrade/detection_s147.py (imports the backtest module for parity, plus cached_zones invalidated by last closed bar), liveTrade/trade_manager_s147.py (MAGIC 1470147, trade_opened/trade_closed plus paper_trade_opened/paper_trade_closed resolving dry-run signals against closed 5m bars, stop-first on straddling bars), liveTrade/engine_s147.py, S147_* config block + .env/.env.example, run.py wiring (--strategy 147). Live-vs-backtest parity replayed on GBPUSD trade #517: direction, model, stop, 4H POI bounds, 15m zone bounds, arrival time and CHoCH time all MATCH; entry differs by design (live fills at market on the confirmation close, backtest at next 5m bar open) and poi_visit counts lower live because the rolling window starts later than full history. All 7 touched/added files compile.
